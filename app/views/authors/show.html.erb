<h1><%= @author.name %></h1>
<!-- Paperclip provides the url method on our avatar so that no matter where
      it's stored, we can always use it in an image_tag -->
<%= image_tag @author.avatar.url(:thumb) %>
<p><%= @author.bio %></p>
<p>Posts:</p>
<% @author.posts.each do |post| %>
  <div><%= post.title %></div>
<% end %>
